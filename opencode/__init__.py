"""OpenCode adapter package.

The adapter is launched through the ``agentis-adapter`` CLI (``app.cli``), which runs
the adapter-initiated WebSocket transport. There is no standalone HTTP launcher.
"""
